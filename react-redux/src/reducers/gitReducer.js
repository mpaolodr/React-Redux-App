// import actions

// initialState
const initialState = {
  isLoading: false,
  user: {},
  error: {}
};


export const gitReducer = (state = initialState, action) {
    switch(action.type) {

        // default case
        default: return state;
        
    }
}
